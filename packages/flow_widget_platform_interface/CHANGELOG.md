# Changelog

## 1.0.1

- Document that `appGroupId` is iOS/macOS-only and does not select Android
  SharedPreferences.
- Add `FlowWidgetOptions.defaultAndroidPrefsName` (`flutter_flow_widget`).
- Clarify that `useGlance` drives Glance recomposition on Android refresh.

## 1.0.0

- Initial stable release.
